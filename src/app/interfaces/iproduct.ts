export interface IProduct {
  id: string;
  name: string;
  // description: string;
  price: number;
  image: string;
  // category: string;
  // supercategory: string;
  // quantity: number;
  // inStock: boolean;
  // isActive: boolean;
  // isDeleted: boolean;
  // bestSeller: boolean;
}
